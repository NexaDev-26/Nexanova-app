# =========================================
# NexaNova Environment Variables - Production
# =========================================

# Server Configuration
NODE_ENV=production
PORT=5000

# Security
# Generate a secure secret for JWT:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secure-random-jwt-secret-here

# CORS Configuration (comma-separated for multiple origins)
# Allow only your Vercel frontend domain
CORS_ORIGINS=https://nexanova-frontend.vercel.app

# Frontend API URL
# The frontend will use this to call your backend
REACT_APP_API_URL=https://nexanova-app-3mii.onrender.com/api

# Database (SQLite - auto-created in backend/data/)
# No configuration needed for SQLite

# WebSocket (enabled by default)
# No configuration needed
